# meta-ad-library-scraper
Scrape Facebook and Instagram ads from Meta public Ad Library into one clean JSON schema, with built-in change tracking: a scheduled run tells you what is new or changed instead of re-billing the same ads.
